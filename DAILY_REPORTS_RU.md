# Система ежедневных отчётов по сотрудникам

## Описание

Реализована система автоматической генерации ежедневных отчётов по всем сотрудникам с детальной статистикой выполнения задач.

## Формат отчёта

Каждый отчёт содержит следующую информацию:

### 📊 Заголовок
- Дата отчёта
- Имя сотрудника

### ✅ Выполненные задачи
- Количество выполненных задач за день
- Список задач с названиями
- Эмодзи срочности для каждой задачи:
  - 🟥 — высокая срочность (urgent, high)
  - 🟨 — средняя срочность (normal, medium)
  - 🟩 — низкая срочность (low)
- Плановое и фактическое время для каждой задачи

### ⏱️ Статистика по времени
- Суммарное плановое время
- Суммарное фактическое время
- Разница (перерасход или экономия)

### ⚡ Статистика по срочности
Для каждого уровня приоритета:
- Количество выполненных задач
- Количество невыполненных задач

### 📌 Перенесённые задачи
Список задач, у которых дедлайн был на указанную дату, но они не были выполнены.

### ⏳ Просроченные задачи
Список задач, просроченных более чем на 1 день.

## Использование

### Базовая команда (отчёт за сегодня)
```bash
python -m clickup_agent report
```

### Отчёт за конкретную дату
```bash
python -m clickup_agent report --date 2025-10-29
```

### Сохранение в файл
```bash
python -m clickup_agent report --output reports.txt
```

### Автоматическая генерация через cron

Ежедневно в 18:00:
```bash
0 18 * * * cd /path/to/clickup-system && /path/to/.venv/bin/python -m clickup_agent report --output /path/to/reports/daily_$(date +\%Y\%m\%d).txt
```

## Технические детали

### Изменённые файлы

1. **`src/clickup_agent/models.py`**
   - Расширена модель `ClickUpTask` для поддержки:
     - Списка исполнителей (assignees)
     - Времени оценки и фактического времени (time_estimate, time_spent)
     - Дат создания, обновления и закрытия
   - Добавлены методы для работы с приоритетами и временем

2. **`src/clickup_agent/reports.py`** (новый файл)
   - Модель `TaskStats` — статистика по одной задаче
   - Модель `PriorityStats` — статистика по приоритету
   - Модель `EmployeeReport` — отчёт по сотруднику
   - Класс `DailyReportGenerator` — генератор отчётов

3. **`src/clickup_agent/__main__.py`**
   - Добавлена команда `report` в CLI
   - Реализована функция `run_report()` для генерации отчётов
   - Сохранена обратная совместимость с командой `analyze`

4. **`README.md`**
   - Добавлена документация по новой функциональности
   - Обновлены примеры использования
   - Добавлены примеры cron-заданий

5. **`REPORT_EXAMPLE.md`** (новый файл)
   - Подробное описание формата отчёта
   - Примеры использования
   - Требования к данным в ClickUp

### Логика работы

1. **Получение задач**
   - Запрашиваются закрытые задачи (статусы: closed, complete, completed)
   - Запрашиваются активные задачи (статусы: open, in progress, to do)
   - Фильтрация по дате закрытия или дедлайну

2. **Группировка по сотрудникам**
   - Задачи группируются по ID исполнителя
   - Задачи без исполнителя попадают в группу "Без исполнителя"

3. **Анализ задач**
   - Определение выполненных/невыполненных задач
   - Подсчёт времени (плановое vs фактическое)
   - Статистика по приоритетам
   - Выявление перенесённых задач (дедлайн сегодня, но не выполнено)
   - Выявление просроченных задач (просрочка > 1 дня)

4. **Форматирование отчёта**
   - Генерация Markdown-форматированного текста
   - Добавление эмодзи для визуальной идентификации приоритетов
   - Вывод в консоль или сохранение в файл

## Требования к ClickUp

Для корректной работы отчётов необходимо настроить в ClickUp:

1. **Назначение исполнителей** на задачи
2. **Установка приоритетов** (urgent/high/normal/medium/low)
3. **Указание дедлайнов** для задач
4. **Трекинг времени**:
   - Time Estimate (плановое время)
   - Time Tracked (фактическое время)
5. **Правильные статусы** для закрытых задач

## Примеры интеграции

### Отправка в Telegram
```bash
#!/bin/bash
REPORT=$(python -m clickup_agent report)
curl -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
  -d "chat_id=${CHAT_ID}" \
  -d "text=${REPORT}"
```

### Отправка в Slack
```bash
#!/bin/bash
REPORT=$(python -m clickup_agent report)
curl -X POST "${SLACK_WEBHOOK_URL}" \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"${REPORT}\"}"
```

### Отправка по email
```bash
#!/bin/bash
python -m clickup_agent report --output /tmp/report.txt
mail -s "Ежедневный отчёт $(date +%d.%m.%Y)" team@company.com < /tmp/report.txt
```

## Расширение функциональности

### Добавление новых метрик
Для добавления новых метрик в отчёт:
1. Расширьте модель `EmployeeReport` в `reports.py`
2. Обновите метод `_generate_employee_report()`
3. Измените метод `to_markdown()` для отображения новых данных

### Фильтрация по сотрудникам
Можно добавить фильтр для генерации отчётов только по определённым сотрудникам:
```python
# В reports.py, метод generate_reports()
if employee_filter:
    employee_tasks = {
        k: v for k, v in employee_tasks.items() 
        if k in employee_filter
    }
```

### Экспорт в другие форматы
Добавьте методы для экспорта в JSON, CSV, Excel:
```python
def to_json(self) -> str:
    return self.model_dump_json(indent=2)

def to_csv(self) -> str:
    # Реализация экспорта в CSV
    pass
```

## Тестирование

Для тестирования системы отчётов:

1. Убедитесь, что в ClickUp есть задачи с:
   - Назначенными исполнителями
   - Установленными приоритетами
   - Трекингом времени
   - Дедлайнами

2. Запустите команду:
```bash
python -m clickup_agent report --log-level DEBUG
```

3. Проверьте вывод на наличие всех необходимых секций

## Поддержка

При возникновении проблем проверьте:
- Правильность настройки `.env` файла
- Наличие прав доступа к ClickUp API
- Корректность данных в ClickUp (исполнители, приоритеты, время)
- Логи приложения (используйте `--log-level DEBUG`)
