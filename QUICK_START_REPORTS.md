# Быстрый старт: Ежедневные отчёты

## Что было реализовано

Добавлена новая команда `report` для генерации ежедневных отчётов по всем сотрудникам.

## Формат отчёта (как в вашем примере)

```
📊 Отчёт за 28.10.2025
👤 Сотрудник: Иван Петров

✅ Выполнено задач: 10
  Подготовка сметы 🟥 (План: 3ч / Факт: 4ч)
  Встреча с клиентом 🟨 (План: 2ч / Факт: 1,5ч)
  Обновление базы материалов 🟩 (План: 1ч / Факт: 0,8ч)
  ...

⏱️ Время:
  Плановое: 20 ч
  Фактическое: 21,5 ч
  Разница: +1,5 ч

⚡ Срочность:
  Высокая: выполнено 3, не выполнено 1
  Средняя: выполнено 5, не выполнено 2
  Низкая: выполнено 2, не выполнено 0

📌 Перенесено задач по дедлайну: 2
  Подготовка отчёта для инвесторов
  Проверка спецификаций

⏳ Просрочено более чем на 1 день: 1
  Актуализация документации по проекту Lagos
```

## Как использовать

### 1. Отчёт за сегодня (вывод в консоль)
```bash
python -m clickup_agent report
```

### 2. Отчёт за конкретную дату
```bash
python -m clickup_agent report --date 2025-10-29
```

### 3. Сохранение отчёта в файл
```bash
python -m clickup_agent report --output daily_report.txt
```

### 4. Отчёт за вчера с сохранением
```bash
python -m clickup_agent report --date $(date -v-1d +%Y-%m-%d) --output yesterday_report.txt
```

### 5. Только завершённые задачи
```bash
python -m clickup_agent report --status completed
```

### 6. Отчёт по конкретному исполнителю и статусам
```bash
python -m clickup_agent report --assignee 123456789 --status "in progress" --status "review"
```

## Автоматизация через cron

### Ежедневные отчёты в 18:00
```bash
crontab -e
```

Добавьте строку:
```
0 18 * * * cd /Users/danyanovich/code\ projects/clickup-system && /path/to/.venv/bin/python -m clickup_agent report --output /path/to/reports/daily_$(date +\%Y\%m\%d).txt
```

### Еженедельные отчёты по пятницам
```
0 18 * * 5 cd /Users/danyanovich/code\ projects/clickup-system && /path/to/.venv/bin/python -m clickup_agent report --output /path/to/reports/weekly_$(date +\%Y\%m\%d).txt
```

## Что нужно настроить в ClickUp

Для корректной работы отчётов убедитесь, что в ClickUp:

1. ✅ **Задачи назначены на исполнителей** (assignees)
2. ✅ **Установлены приоритеты** задач:
   - Urgent/High → 🟥 (красный)
   - Normal/Medium → 🟨 (жёлтый)
   - Low → 🟩 (зелёный)
3. ✅ **Указаны дедлайны** для задач
4. ✅ **Включен трекинг времени**:
   - Time Estimate (плановое время)
   - Time Tracked (фактическое время)
5. ✅ **Задачи закрываются** со статусами: "closed", "complete", "completed"

## Интеграция с мессенджерами

### Отправка в Telegram
Создайте скрипт `send_to_telegram.sh`:
```bash
#!/bin/bash
REPORT=$(python -m clickup_agent report)
curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
  -d "chat_id=${TELEGRAM_CHAT_ID}" \
  -d "text=${REPORT}" \
  -d "parse_mode=HTML"
```

Запуск через cron:
```
0 18 * * * /path/to/send_to_telegram.sh
```

### Отправка в Slack
Создайте скрипт `send_to_slack.sh`:
```bash
#!/bin/bash
REPORT=$(python -m clickup_agent report)
curl -X POST "${SLACK_WEBHOOK_URL}" \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"${REPORT}\"}"
```

## Примеры использования

### Отчёт за последнюю неделю
```bash
for i in {0..6}; do
  date=$(date -v-${i}d +%Y-%m-%d)
  python -m clickup_agent report --date $date --output "report_${date}.txt"
done
```

### Отчёт с отправкой по email
```bash
python -m clickup_agent report --output /tmp/report.txt
mail -s "Ежедневный отчёт $(date +%d.%m.%Y)" manager@company.com < /tmp/report.txt
```

### Отчёт только для определённой даты с логами
```bash
python -m clickup_agent report --date 2025-10-29 --log-level DEBUG
```

## Структура проекта после изменений

```
clickup-system/
├── src/
│   └── clickup_agent/
│       ├── __init__.py
│       ├── __main__.py          # ✨ Обновлён: добавлена команда report
│       ├── clickup.py
│       ├── config.py
│       ├── gpt.py
│       ├── models.py            # ✨ Обновлён: расширена модель ClickUpTask
│       ├── orchestrator.py
│       └── reports.py           # ✨ Новый: генератор отчётов
├── README.md                    # ✨ Обновлён: добавлена документация
├── REPORT_EXAMPLE.md            # ✨ Новый: примеры отчётов
├── DAILY_REPORTS_RU.md          # ✨ Новый: подробная документация
└── QUICK_START_REPORTS.md       # ✨ Этот файл
```

## Проверка работы

1. Убедитесь, что `.env` файл настроен:
```bash
cat .env
# Должны быть заполнены:
# CLICKUP_API_TOKEN=...
# CLICKUP_LIST_ID=... или CLICKUP_TEAM_ID=...
```

2. Запустите тестовую генерацию:
```bash
python -m clickup_agent report --log-level DEBUG
```

3. Проверьте вывод:
   - Должны быть отчёты по всем сотрудникам
   - Каждый отчёт содержит все секции
   - Эмодзи отображаются корректно

## Troubleshooting

### Проблема: "No tasks found"
**Решение**: Проверьте, что в ClickUp есть задачи за указанную дату с назначенными исполнителями.

### Проблема: "Отчёты пустые"
**Решение**: Убедитесь, что задачи имеют:
- Назначенных исполнителей
- Статус "closed" или "completed"
- Дату закрытия (date_closed)

### Проблема: "Время не отображается"
**Решение**: В ClickUp должны быть заполнены поля:
- Time Estimate (плановое время)
- Time Tracked (фактическое время)

### Проблема: "Эмодзи не отображаются"
**Решение**: Убедитесь, что терминал поддерживает UTF-8. Попробуйте:
```bash
export LANG=ru_RU.UTF-8
python -m clickup_agent report
```

## Дополнительные возможности

### Фильтрация по сотрудникам (будущая функция)
Можно добавить фильтр для генерации отчётов только по определённым сотрудникам.

### Экспорт в разные форматы (будущая функция)
Планируется добавить экспорт в JSON, CSV, Excel.

### Графики и визуализация (будущая функция)
Можно добавить генерацию графиков по времени и приоритетам.

## Обратная связь

Если у вас есть предложения по улучшению отчётов или нужны дополнительные метрики, создайте issue в репозитории или свяжитесь с разработчиком.
